[Unit]
Description=Spaceflight launch tracker (refresh + notifications)
After=network-online.target graphical-session.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=%h/Projects/spaceflight/scripts/spaceflight daemon
Restart=on-failure
RestartSec=30
Environment=PYTHONUNBUFFERED=1
# Include ~/.local/bin so notify-click can find spaceflight + terminal helpers
Environment=PATH=%h/.local/bin:%h/.local/share/omarchy/bin:/usr/local/bin:/usr/bin:/bin

[Install]
WantedBy=default.target
